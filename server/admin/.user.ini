; Upload limits for the admin panel's APK publisher (see ../.user.ini).
; Placed here too so it applies even if parent .user.ini isn't scanned.
upload_max_filesize = 200M
post_max_size = 210M
memory_limit = 256M
max_execution_time = 600
max_input_time = 600
