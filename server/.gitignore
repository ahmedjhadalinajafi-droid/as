# Never commit the Firebase service-account key
service-account.json

# Don't commit uploaded images (keep the folder + its .htaccess)
uploads/*
!uploads/.htaccess
pending_build.json
build_status.json
